define(function (require) {
    describe('Backbone.HypermediaCollection', function() {
        it('should be defined', function() {
            expect(Backbone.HypermediaCollection).toBeDefined();
        });
    });
});